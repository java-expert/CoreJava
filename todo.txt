heap, stringconstant pool, stack ------------Memory allocation again

....................................................................
    String s = new String("Welcome");  

Two objects, one in string constant pool and other in non-pool(heap). 
....................................................................